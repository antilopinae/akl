#ifndef ANT_DOUBLE_H
#define ANT_DOUBLE_H

#include "types.h"

static inline akl_u64 akl_d64_add_bits(akl_u64 a_bits, akl_u64 b_bits) {
    // I DONT KNOW HOW
    return a_bits + b_bits;
}

static inline akl_u64 akl_d64_sub_bits(akl_u64 a_bits, akl_u64 b_bits) {
    // i DONT KNOW HOW
    return a_bits - b_bits;
}

#endif
