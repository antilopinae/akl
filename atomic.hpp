#pragma once

#include <stdint.h>

#include <boost/type_traits/is_integral.hpp>

namespace ant {
    namespace detail {
        template<typename T, bool IsIntegral>
        class atomic_impl {
        public:

        };


    }
}
